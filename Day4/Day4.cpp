#include <iostream>
#include "../Program/Function.h"

using namespace std;

int main()
{
#pragma region 상속
	//상위 클래스의 속성을 하위 클래스가 사용할 수 있도록 설정해주는 기능입니다.


	/*Consumable obj;*/

	//Function obj2;

	//cout << "Consumable 사이즈 : " << sizeof(obj) << endl;
	//cout << "Function 사이즈 : " << sizeof(obj2) << endl;

	// 클래스의 상속 관계에서 상위 클래스는 하위 클래스의 속성을
	// 사용할 수 없습니다. 하위 클래스는 상위 클래스의 메모리가 포함된
	// 상태로 메모리의 크기가 결정됩니다.


#pragma endregion

#pragma region 다향성

#pragma endregion
	return 0;
}